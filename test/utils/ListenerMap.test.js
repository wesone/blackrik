const ListenerMap = require('../../src/utils/ListenerMap');

test('ListenerMap execute', () => {
    // const listenerMap = new ListenerMap();
    // listenerMap.add('type1', (...args) => {});
    // listenerMap.execute('type1', 'argument1', 'argument2');
    expect(() => {}).not.toThrow();
});
